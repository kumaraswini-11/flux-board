export function ChangePassword() {
    return (
        <div>
            <h1>Change Password</h1>
        </div>
    )
}

// new password, confirm password   - old password?
// profile setion

// name ,image, - noy allow email chnge?
// allow delete account


// cret toggel otp form switch ,  by default two  fcator false.
// add dialog for conformation,  ther will ask for confirm your email, and give email inpt, after emil clcik enagel buton.
/**
 * const res = awit authClient.twoFactor.enable({
    password:password,
    
 })

    or
const res = awit authClient.twoFactor.disable({
    password:password,
  
 })



    if(res.error){
        toast({
            title: "Error",
            description: res.error.message,
            variant: "destructive",
        })
    }
        toast({
            title: "Success",
            description: "Two factor authentication enabled",
            variant: "default",
        })
            router.refersh(); // upadte the thing in the smae page
 */

/**
 * sign.tsx
 * 
 * onSucess: async = ()=>{
 * // remove the success message
 * 
 * s
 * }
 */